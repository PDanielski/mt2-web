{% extends 'pages/settings/settings.html.twig' %}
{% block settingsBody %}
    <div class="title-2 centered">Change password</div>
    <div id="settings-change-password-form"></div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript">
        const options = {
            successUrl: "{{ path('getChangePasswordSuccessAction')|escape('js') }}",
            apiUrl: "{{ path('apiChangePasswordAction')|escape('js') }}"
        };
        const elem = document.getElementById("settings-change-password-form");
        loadComponent('settingsChangePasswordForm', elem, options);
    </script>
{% endblock %}